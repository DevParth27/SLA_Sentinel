# Dependencies for the aipipeline service. Kept in sync with
# config/requirements.txt. Inlined here (no `-r` include) because Vercel's
# @vercel/python builder cannot follow include directives.

# Core AI & LLM
openai==2.42.0

# PDF parsing
pdfplumber==0.11.0

# API server
fastapi==0.111.0
uvicorn==0.30.1
python-multipart==0.0.9

# AWS (S3 download)
boto3==1.34.131

# Utilities
python-dotenv==1.0.1
pydantic==2.7.4
